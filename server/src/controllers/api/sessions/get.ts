import { Request, Response } from "express"
export const getSessionController = (req: Request, res: Response) => {
    
}

export const getSessionsController = (req: Request, res: Response) => {
    
}